import * as React from "react";

export interface GifPanelProps {}

const GifPanel: React.SFC<GifPanelProps> = () => {
  return (
    <div className="h-full w-full bg-purple-400">
      G
    </div>
  );
};

export default GifPanel;
